import React, {Fragment} from 'react'

export const About = () => {
    return (
        <Fragment>
            <h1>About this App</h1>
            <p>App to search GitHub users.</p>
            <p>Version 1.0.0 - using react-router-dom@5, which isn't most current.</p>
        </Fragment>
    );
};

export default About;
